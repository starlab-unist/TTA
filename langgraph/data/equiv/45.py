def calculate_triangle_area(base, height):
    area = (base * height) / 2.0
    return area
# method name: triangle_area -> calculate_triangle_area
# parameter name: a -> base, h -> height
# introduced a new variable 'area' to store the result before returning
# kept the calculation and return statement functionally the same