def compute_sum(a: int, b: int) -> int:
    total = a + b
    return total
# method name: add -> compute_sum
# parameter names: x -> a, y -> b
# introduced a new variable 'total' to store the sum before returning
# added explicit return type annotation -> int